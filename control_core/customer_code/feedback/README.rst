Feedback control
################

現在は制御演算を行わないパススルー実装です。``feedback_control_duty_from_adc()`` が
16-bit ADC値を0..100%のPWM dutyへ線形変換します。

将来、顧客固有のPI/PID、電圧・電流制御、保護判定をこのディレクトリへ実装します。
処理は制御周期内に必ず完了させ、sleep、ネットワークアクセス、無期限待機を避けてください。
